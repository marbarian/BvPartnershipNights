@model Capstone.WebUI.Domain.Entities.Charity

@{
    ViewBag.Title = "Edit";
}

<h1>Edit @Model.Name</h1>

@using (Html.BeginForm("CharityEdit", "CRUD")) {

    @Html.EditorForModel()
    <input type="submit" value="Save" />

    @Html.ActionLink("Cancel and return to List", "CharityIndex")
}


